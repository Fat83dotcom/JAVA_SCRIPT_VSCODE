let listaFilmes = ['Matrix', 'O jogo da imitação', 'Dragão Vermelho']

for (let posicaoFilme = 0; posicaoFilme < listaFilmes.length; posicaoFilme++) {
    document.write("<p>", listaFilmes[posicaoFilme], "</p>")
    console.log(listaFilmes[posicaoFilme]);
}
